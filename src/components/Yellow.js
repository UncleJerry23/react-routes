import React from 'react';

function Yellow() {
  const blockStyle = {
    height: '500px',
    width: '500px',
    backgroundColor: 'yellow'
  };
  
  return <div style={blockStyle} />;
}

export default Yellow;
