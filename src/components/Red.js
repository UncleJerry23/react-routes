import React from 'react';

function Red() {
  const blockStyle = {
    height: '500px',
    width: '500px',
    backgroundColor: 'red'
  };
  
  return <div style={blockStyle} />;
}

export default Red;
